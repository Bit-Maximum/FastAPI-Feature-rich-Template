---
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: app
  name: app-db
spec:
  selector:
    matchLabels:
      app: app-db
  template:
    metadata:
      labels:
        app: app-db
    spec:
      containers:
        - name: database
          image: postgres:16.3-bullseye
          resources:
            limits:
              memory: "300Mi"
              cpu: "200m"
          env:
            - name: POSTGRES_PASSWORD
              value: "app"
            - name: POSTGRES_USER
              value: "app"
            - name: POSTGRES_DB
              value: "app"
          ports:
            - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  namespace: app
  name: "app-db-service"
spec:
  selector:
    app: app-db
  ports:
    - port: 5432
      targetPort: 5432
---
apiVersion: batch/v1
kind: Job
metadata:
  namespace: app
  name: app-migrator
spec:
  ttlSecondsAfterFinished: 100
  template:
    spec:
      containers:
      - name: migrator
        image: app:latest
        command:
          - "alembic"
          - "upgrade"
          - "head"
        resources:
          limits:
            memory: "200Mi"
            cpu: "250m"
        env:
          - name: "APP_DB_HOST"
            value: "app-db-service"
      initContainers:
        - name: wait-for-db
          image: toschneck/wait-for-it:latest
          command: ["./wait-for-it.sh", "-t", "60", "app-db-service:5432"]
      restartPolicy: Never

---
