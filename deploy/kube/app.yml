---
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: app
  name: app-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: app-app
  template:
    metadata:
      labels:
        app: app-app
    spec:
      containers:
        - name: app
          image: app:latest
          readinessProbe:
            httpGet:
              path: /api/health
              port: api-port
            initialDelaySeconds: 5
            periodSeconds: 10
          env:
            - name: APP_HOST
              value: "0.0.0.0"
            - name: APP_WORKERS_COUNT
              value: "10"
            - name: APP_DB_HOST
              value: "app-db-service"
            - name: APP_REDIS_HOST
              value: "app-redis-service"
          resources:
            limits:
              memory: "200Mi"
              cpu: "100m"
          ports:
            - containerPort: 8000
              name: api-port
---
apiVersion: v1
kind: Service
metadata:
  namespace: app
  name: app-app-service
spec:
  selector:
    app: app-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: api-port
      name: api-port

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-app
  namespace: app
  labels:
    name: app-app
spec:
  rules:
    - host: app.local
      http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: app-app-service
                port:
                  name: api-port

---
